import React from 'react';
import { Typography, Container } from '@mui/material';

const Header = () => {
  return (
    <Container>
        <Typography variant='h4'>
            The surest chances of liberation lie in what is most familiar.
        </Typography>
    </Container>
  )
}

export default Header